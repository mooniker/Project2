<h2><%= @project.title %></h2> <%= link_to '(edit)', edit_user_project_path(current_user, @project) %>

<% @project.tasks.each do |task| %>
<div class="task"><%= link_to task.name, user_project_task_path(current_user, @project, task) %></div>
<% end %>
